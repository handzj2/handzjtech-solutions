# DEFECT LOG

| Date | Defect | Found by | Fix |
|---|---|---|---|
| 2026-09-14 | `tsconfig.json` `include` referenced `next-env.d.ts`, which Next.js normally auto-generates on first `dev`/`build` run. Since it never existed in this repo and `npm run typecheck` calls `tsc --noEmit` directly (not `next build`), typecheck would have failed on a missing-file error unrelated to actual code correctness. | Manual code inspection (no toolchain available to catch this via a real `tsc` run) | Added `next-env.d.ts` with the standard Next.js-generated content |
| 2026-09-14 | AUDIT-F1 (MEDIUM): `components/MobileNav.tsx` closes on link click but has no Escape-key handler. COMPONENT_CONTRACT.md requires the mobile navigation to close on "route change/Escape." | Frozen-implementation audit | **Fixed** — owner authorized. Added a keydown listener (attached only while the panel is open, removed on close/unmount) that closes the panel on Escape. |
| 2026-09-14 | AUDIT-F2 (MEDIUM): `app/solutions/page.tsx`, `app/products/page.tsx`, `app/projects/page.tsx` use `uppercase tracking-wide` category/eyebrow labels on cards. DESIGN_SYSTEM.md's "Explicitly Avoided" section names "tracked-out all-caps eyebrow labels" as a generic-SaaS pattern to avoid; these labels were added in Increment 2 without re-checking against that list. | Frozen-implementation audit | **Fixed (restyled)** — owner chose restyle over removal. Dropped `uppercase tracking-wide`; labels now render in the source data's plain case (`text-xs font-semibold text-brand`) in all three pages. |
| 2026-09-14 | AUDIT-F3 (LOW): Increment 2 added a `category` field to products and a `description` field to automation categories in `lib/content.ts`. These are presentational (not new facts) and were reasoned about in PROJECT_SCOPE.md's Increment 2 Scope note, but the additions themselves were never recorded as a discrete entry in DECISIONS.md, and CONTENT_CONTRACT.md was not updated to explicitly cover them. | Frozen-implementation audit | **Documented** — owner approved. Added a "Presentational Fields" section to CONTENT_CONTRACT.md covering both fields, and a DECISIONS.md entry recording the approval. No code change (no fabricated facts were found). |
| 2026-09-14 | AUDIT-F4 (INFORMATIONAL): `components/Header.tsx` imports sibling components via relative paths (`./Container`, `./Button`, `./MobileNav`) while every page and every other component import consumer uses the `@/components/...` alias. Both resolve correctly under `tsconfig.json`'s `paths` config; this is a style inconsistency, not a functional defect. | Frozen-implementation audit | **Fixed** — owner authorized cleanup. `Header.tsx` now uses `@/components/...` for all three imports. Also found and fixed the same pattern in `Footer.tsx` (`./Container`, not separately flagged by the audit) for full consistency. |
| 2026-09-14 | AUDIT-F5 (BLOCKED): `npm install` fails with HTTP 403 from the sandbox's egress proxy — no outbound access to the npm registry in the implementation environment. Blocks `typecheck`/`build`/`lint`. | Frozen-implementation audit / PHASE_STATUS.md | **Resolved — registry access available in this session's environment.** `npm install`, `npm run typecheck`, `npm run build`, and `npm run lint` were all run successfully (see PHASE_STATUS.md). This does not retroactively validate the environment referenced in the original finding — it reflects that this later session had registry access where the earlier one did not. |
| 2026-09-14 | BUILD-F1 (was masked by AUDIT-F5): `app/about/page.tsx` had three raw apostrophes inside JSX text nodes (`don't`, `it's`, `you'd`), which `next build`'s bundled ESLint config flags as `react/no-unescaped-entities` and treats as a build-blocking error. Undetectable by manual inspection or by `tsc --noEmit` alone; only surfaced once `npm run build` could actually run. | Real `npm run build` run, now that registry access is available | **Fixed** — escaped the three apostrophes as `&apos;`. No wording or meaning changed. Build now completes and produces a full static export of all 12 routes. |
